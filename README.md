# Wasmium
A webassembly plugin runtime that is portable between native and web without requiring platform-specific code. Supports two way interop between host and guest, including host functions. Panic handler and log function are included by default.

